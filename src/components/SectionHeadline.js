import styled from "styled-components";

const SectionHeadline = styled.h1`
  font-weight: 500;
  font-size: 48px;
  text-align: left;
  padding: 2rem 0;
  margin: 0;
`;

export default SectionHeadline;
