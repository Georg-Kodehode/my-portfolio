import styled from "styled-components";

const AlignFooter = styled.p`
  display: flex;
  align-items: center;

  &:hover svg path {
    fill: #47b5ff;
  }
  &:hover span {
    color: #47b5ff;
  }
`;

export default AlignFooter;
